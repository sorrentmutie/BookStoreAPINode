'use strict';

const db = require('../database');

/**
 * Restituisce tutti i generi (lista non paginata — sono pochi).
 * @returns {object[]}
 */
function listAll() {
  // TODO
}

/**
 * @param {number} id
 * @returns {object|null}
 */
function findById(id) {
  // TODO
}

module.exports = { listAll, findById };
